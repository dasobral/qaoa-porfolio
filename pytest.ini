[pytest]
# Pytest configuration for QAOA Portfolio Optimizer

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Markers for test categorization
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow-running tests (reserved for Phase 5 benchmarks; currently unused)
    network: Tests requiring network access (reserved for Phase 5 market-data studies; currently unused)
    performance: Performance benchmarking tests (reserved for Phase 5; currently unused)

# Output configuration
addopts =
    --verbose
    --tb=short
    --strict-markers
    --disable-warnings
    --color=yes
    --durations=10

# Test filtering
filterwarnings =
    ignore::UserWarning:yfinance.*
    ignore::DeprecationWarning:pandas.*
    ignore::FutureWarning:pandas.*

# Minimum version
minversion = 7.4

# Coverage configuration (when using pytest-cov)
# addopts = --cov=qaoa_portfolio --cov-report=html --cov-report=term-missing

# Timeout for tests (requires pytest-timeout)
# timeout = 300

# Parallel execution (requires pytest-xdist)
# addopts = -n auto
